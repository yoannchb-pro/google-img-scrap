import Config from "../types/config";
/**
 * Build google dork string based on the config query
 * @param config
 * @returns
 */
declare function buildGoogleDorks(config: Config): string;
export default buildGoogleDorks;
